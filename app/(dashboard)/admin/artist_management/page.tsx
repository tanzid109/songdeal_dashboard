import ArtistTable from '@/dashboard/artist/ArtistTable';
import React from 'react';

const page = () => {
    return (
        <div>
            <ArtistTable />
        </div>
    );
};

export default page;