import PasswordForm from '@/dashboard/settings/PasswordForm';
import React from 'react';

const page = () => {
    return (
        <div>
            <PasswordForm/>
        </div>
    );
};

export default page;